function TinyChip({ children }) {
  return (
    <span className="inline-flex items-center rounded-full border border-zinc-700 bg-zinc-900/90 px-3 py-1 text-xs text-zinc-300">
      {children}
    </span>
  )
}

export default TinyChip
